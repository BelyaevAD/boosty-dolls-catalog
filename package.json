{
  "name": "boosty-dolls-catalog",
  "version": "1.0.0",
  "private": true,
  "description": "Публичный каталог русскоязычных кукольных авторов, подписок и разовых материалов на Boosty.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "update:data": "node scripts/update-data.mjs",
    "update:data:full": "node scripts/full-refresh.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "validate:build": "node scripts/validate-build.mjs",
    "build": "node scripts/build-site.mjs",
    "test": "node --test",
    "check": "npm run validate:data && npm test && npm run build && npm run validate:build"
  }
}
